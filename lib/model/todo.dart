class Todo {
  String id;
  String title;
  bool isDone;
  DateTime timestamp;

  Todo(
      {required this.id,
      required this.title,
      required this.isDone,
      required this.timestamp});
}
